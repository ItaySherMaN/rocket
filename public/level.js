class Level {
	constructor(planets_at_once, planet_min_speed, planet_max_speed) {
		this.planets_at_once = planets_at_once
		this.planet_min_speed = planet_min_speed
		this.planet_max_speed = planet_max_speed
	}
}
